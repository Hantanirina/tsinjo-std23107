spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/tsinjo_preprod
    username: postgres
    password: root
  flyway:
    locations: classpath:db/migration
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
